import 'package:flutter/material.dart';

class TextStyles {
  static const mainStyle = TextStyle(fontSize: 20, );

  static const secondaryStyle = TextStyle(fontSize: 16);
  static const tertiaryStyle = TextStyle(fontSize: 12, color: Color.fromRGBO(154, 154, 154, 1));
}
